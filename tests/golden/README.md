# tests/golden

Golden output fixtures (expected IL dumps, expected `Call`/`CallJSON`
results, expected `vmnet check` reports) used by table-driven tests once the
loader/interpreter exist. Empty until Fase 1.
