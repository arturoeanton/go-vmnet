# calculator

Slightly larger arithmetic/loop example used for benchmarking the
interpreter (Fase 4 benchmark suite) against a native Go equivalent and, on
supported hosts, against CoreCLR. See `docs/ROADMAP.md`.

Not runnable yet.
