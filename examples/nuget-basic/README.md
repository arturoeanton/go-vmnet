# nuget-basic

Loads a certified NuGet package via `vm.NuGet().Add/Restore` and calls one
of its functions from Go. This is the Fase 3 demo scenario — see
`docs/ROADMAP.md`.

Not runnable yet: depends on the `/nuget` resolver and the certified-package
list, which land in Fase 3.
